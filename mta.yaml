_schema-version: 3.3.0
ID: Enterprise-Operations-Innovation-Hub
version: 1.0.0
description: "A simple CAP project."
parameters:
  enable-parallel-deployments: true
  deploy_mode: html5-repo
build-parameters:
  before-all:
    - builder: custom
      commands:
        - npm ci
        - npx cds build --production
modules:
  - name: Enterprise-Operations-Innovation-Hub-srv
    type: nodejs
    path: gen/srv
    parameters:
      instances: 1
      buildpack: nodejs_buildpack
    build-parameters:
      builder: npm-ci
    provides:
      - name: srv-api # required by consumers of CAP services (e.g. approuter)
        properties:
          srv-url: ${default-url}
    requires:
      - name: Enterprise-Operations-Innovation-Hub-destination

  - name: Enterprise-Operations-Innovation-Hub-app-deployer
    type: com.sap.application.content
    path: gen
    build-parameters:
      build-result: app/
      requires:
    requires:
      - name: srv-api
      - name: Enterprise-Operations-Innovation-Hub-html5-repo-host
        parameters:
          content-target: true
          config:
            HTML5Runtime_enabled: true
    parameters:
      config:
        destinations:
          - Name: srv-api
            URL: ~{srv-api/srv-url}
            Authentication: NoAuthentication
            Type: HTTP
            ProxyType: Internet
            HTML5.ForwardAuthToken: true
            HTML5.DynamicDestination: true

  - name: Enterprise-Operations-Innovation-Hub-destinations
    type: com.sap.application.content
    requires:
      - name: Enterprise-Operations-Innovation-Hub-html5-repo-host
        parameters:
          service-key:
            name: Enterprise-Operations-Innovation-Hub-html5-repo-host-key
      - name: srv-api
      - name: Enterprise-Operations-Innovation-Hub-destination
        parameters:
          content-target: true
    build-parameters:
      no-source: true
    parameters:
      content:
        instance:
          existing_destinations_policy: update
          destinations:
            - Name: Enterprise-Operations-Innovation-Hub-html5-repository
              ServiceInstanceName: Enterprise-Operations-Innovation-Hub-html5-repo-host
              ServiceKeyName: Enterprise-Operations-Innovation-Hub-html5-repo-host-key
              sap.cloud.service: EnterpriseOperationsInnovationHub.service

resources:
  - name: Enterprise-Operations-Innovation-Hub-destination
    type: org.cloudfoundry.managed-service
    parameters:
      service: destination
      service-plan: lite
    requires:
      - name: srv-api

  - name: Enterprise-Operations-Innovation-Hub-html5-repo-host
    type: org.cloudfoundry.managed-service
    parameters:
      service: html5-apps-repo
      service-plan: app-host
